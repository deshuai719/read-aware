# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
!plugins/*/dist/
!plugins/*/dist/**
.tanstack
storybook-static
*.local
.eval/

# Turborepo / Cloudflare local state
.turbo
.wrangler

# Tauri / Rust
apps/desktop/src-tauri/target
# Generated IPC permission schemas only — gen/android and gen/apple are the
# mobile project templates and are meant to be committed (each carries its own
# .gitignore for build outputs).
apps/desktop/src-tauri/gen/schemas
target

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
demo

# Local build deliverables (installers live in GitHub Releases)
/installers/
